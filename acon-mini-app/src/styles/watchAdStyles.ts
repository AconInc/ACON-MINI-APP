import { StyleSheet } from 'react-native';

export const watchAdStyles = StyleSheet.create({
  lottieContainer: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  infoContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 6,
    marginBottom: 26,
  },
});
